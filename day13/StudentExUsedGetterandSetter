# SourceCode
check : 코드길이 줄이기
generate getter and setter 통해 만들었더니 코드가 약간.. 중구난방 같다
check2 : test02 클래스를 따로 만들어서 거기에 main() 했더니 실행이 안됐다. 선생님이 main() 메소드는 public class 안에 들어가야 한다고 하셨다.
앞으로는 public class 안에 main() 있는지 확인하기.

```java
package day13.test;

import javax.swing.JOptionPane;

public class Student {
	// field
	private String name;
	private int kor;
	private int eng;
	private int math;
	private int aver;
	private char grade = 'F';

//	//constructors
//	Student(String name){
//		this(name, 0, 0, 0);
//	}
//
//	Student(int k, int e, int m){
//		this("없음", k, e, m);
//	}
//
//	Student(String name, int k, int e, int m){
//		this.setData(name, k, e, m);
//		this.setGrade();
//	}

	// getters and setters
	public String getName() {
		return name;
	}

	public void setName(String name) {
		if (name.length() > 5) {
			return;
		}
		this.name = name;
	}

	public int getKor() {
		return kor;
	}

	public void setKor(int kor) {
		if (kor > 100) {
			return;
		}
		this.kor = kor;
	}

	public int getEng() {
		return eng;
	}

	public void setEng(int eng) {
		if (eng > 100) {
			return;
		}
		this.eng = eng;
	}

	public int getMath() {
		return math;
	}

	public void setMath(int math) {
		if (math > 100) {
			return;
		}
	}

	public int getAver() {
		return aver;
	}

	public void setAver(int aver) {
		this.aver = (this.getKor() + this.getEng() + this.getMath()) / 3;
		this.setGrade();
	}

	public char getGrade() {
		return grade;
	}

	public void setGrade() {
		switch (this.getAver() / 10) {
		case 10:
		case 9: {
			this.grade = 'A';
			return;
		}
		case 8: {
			this.grade = 'B';
			return;
		}
		case 7: {
			this.grade = 'C';
			return;
		}
		case 6: {
			this.grade = 'D';
			return;
		}
		}
	}

	// method
	String getData() {
		return "* 학생 정보 출력 *\n이름 : " + this.name + "\n국어 점수 : " + this.kor + "\n영어 점수 : " + this.eng + "\n수학 점수 : "
				+ this.math + "\n평균 : " + this.aver + "\n등급 : " + this.grade;
	}

	void setData(String name, int kor, int eng, int math) {
		this.setName(name);
		this.setKor(kor);
		this.setEng(eng);
		this.setMath(math);
		this.setAver((kor + eng + math) / 3);
		this.setGrade();
	}
	public static void main(String[] args) {
		Student[] students = new Student[4];
		String message = "";

		for (int i = 0; i < students.length; i++) {
			String name = JOptionPane.showInputDialog(null, "학생 이름을 입력하세요");
			int kor = Integer.parseInt(JOptionPane.showInputDialog(null, "국어 점수를 입력하세요"));
			int eng = Integer.parseInt(JOptionPane.showInputDialog(null, "영어 점수를 입력하세요"));
			int math = Integer.parseInt(JOptionPane.showInputDialog(null, "수학 점수를 입력하세요"));

			students[i] = new Student();
			students[i].setData(name, kor, eng, math);

		} // 배열 길이동안 입력받은 학생 정보를 토대로 Student 객체 생성 후 배열에 넣는다.

		JOptionPane.showMessageDialog(null, "학생 정보 입력 완료!");
		for (Student s : students) {
			message += s.getData();
		}
		JOptionPane.showMessageDialog(null, message);
	}
}

```
